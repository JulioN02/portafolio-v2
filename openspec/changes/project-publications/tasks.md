# Tasks: Project Publications

> **Strict TDD (api)**: per spec scenario write failing tests first (RED) → implement minimum (GREEN) → TRIANGULATE edge cases → REFACTOR. Runner `pnpm --filter @jsoft/api test` (alias `pnpm --filter api test`); coverage gate `pnpm --filter @jsoft/api exec jest --coverage` (≥70%). Typecheck gate per phase `pnpm -r run typecheck` (0 errors). Frontend Vitest where sensible; shared schema tests via existing `packages/shared/src/schemas/__tests__` pattern. Deploy order per phase: shared → prisma migrate → api → admin → client/recruiter. Per-phase rollback: revert migration + unregister routes (additive, no data loss).

## Phase 1: Core — Project chain + aggregation relocation (WEIGHT: LARGE → 2 chained PRs: P1a shared+api, P1b admin+client+recruiter)

^- [x] **P1-01** Prisma `Project` model (no type enum; `tags String[]`, featured/order, PostStatus, deletedAt/publishedAt, 3 indexes) + `migrate dev --name add_project_model`. REQ: projects:Model. Files: `api/prisma/schema.prisma`. Test: schema inspect.
^- [x] **P1-02** Shared `project.schema.ts`: projectSchema, projectUpdateSchema, projectFilterSchema (status/tag/search/page/limit), projectStatusSchema, tagsSchema (trimmed 1–30 chars, max 10) + exports. REQ: projects:SharedSchemas. Files: `packages/shared/src/schemas/project.schema.ts`, `index.ts`. Test: schema tests (valid input; empty tag / 11 tags rejected).
^- [x] **P1-03** Shared `sanitizeHtml(html,{allowMedia})` util (DOMPurify; allow img/video/source/figure/figcaption; iframe ONLY when src matches `^/api/simulators/[A-Za-z0-9]+/content$`; scripts stripped). REQ: sanitize:AllRenderers/ScriptsStripped. Files: `packages/shared/src/utils/sanitize.ts`. Test: strip/preserve assertions.
^- [x] **P1-04** RED `project.service.test.ts`: CRUD, PUBLISHED-only list/detail, soft-delete+restore, tag filter, `/tags`, publishedAt logic, slug 404. REQ: projects:APICRUD. Files: `api/src/services/project.service.test.ts`. Test: suite RED.
^- [x] **P1-05** GREEN `project.service.ts` (findAll/findBySlug/findById/create/update/softDelete/restore/status/reorder/getTags). REQ: projects:APICRUD. Files: `api/src/services/project.service.ts`. Test: suite GREEN.
^- [x] **P1-06** `project.controller.ts` + `project.routes.ts` (register `/tags` & `/by-id/:id` BEFORE `/:slug`) + mount `app.ts`; integration tests 401-without-JWT + route order. REQ: projects:APICRUD. Files: `api/src/{controllers,routes}/project.*`, `api/src/app.ts`. Test: controller/integration.
^- [x] **P1-07** RED `portfolio.service.test.ts`: status-leak regression (EVERY source PUBLISHED + deletedAt null), lab posts included via category proxy (laboratorio/experimento), articulo excluded, real Project rows, pagination, classification filter. REQ: recruiter:Listing. Files: `api/src/services/portfolio.service.test.ts`. Test: RED.
^- [x] **P1-08** GREEN `portfolio.service.ts`: merge Project + Service/Product/Tool/SuccessCase + lab BlogPosts (category proxy until P2-06), sort createdAt desc, paginate. REQ: recruiter:Listing. Files: `api/src/services/portfolio.service.ts`. Test: GREEN.
^- [x] **P1-09** `portfolio.controller.ts` + `portfolio.routes.ts` (`/api/portfolio/projects`, `/recent`, `/classifications`) + mount `app.ts`; DELETE old `projects.controller/service/routes.ts`. REQ: recruiter:Listing/DetailModal. Files: `api/src/{controllers,services,routes}/portfolio.*`; delete `projects.*`. Test: integration; no stale refs.
- [x] **P1-10** Seed demo Projects (tags-based classification) in `api/prisma/seed.ts`. REQ: projects:Model. Test: seed runs.
- [x] **P1-11** Admin `api/projects.api.ts` + `hooks/useProjects.ts` (list/create/update/softDelete/restore/status/reorder/tags). REQ: projects:AdminPages. Files: `admin-panel/src/api/projects.api.ts`, `admin-panel/src/hooks/useProjects.ts`. Test: typecheck.
- [x] **P1-12** Admin `ProjectTable.tsx` + `ProjectsListPage.tsx` + routes/sidebar/i18n. REQ: projects:AdminPages. Files: `admin-panel/src/components/projects/ProjectTable.tsx`, `admin-panel/src/pages/projects/ProjectsListPage.tsx`. Test: typecheck.
- [x] **P1-13** Admin `ProjectCreatePage.tsx` (title/slug/shortDescription/body via existing TipTapEditor/images/repositoryUrl/tags minimal chips/status/featured/order → POST). REQ: projects:AdminPages. Files: `admin-panel/src/pages/projects/ProjectCreatePage.tsx`. Test: typecheck; manual create.
- [x] **P1-14** Admin `ProjectEditPage.tsx` (prefill incl. tags; PUT; PATCH status/restore/reorder). REQ: projects:AdminPages. Files: `admin-panel/src/pages/projects/ProjectEditPage.tsx`. Test: typecheck; manual prefill.
- [x] **P1-15** Client `hooks/useProjects.ts` (list + tag filter, useProjectTags, useProjectBySlug). REQ: projects:ClientPages. Files: `client-site/src/hooks/useProjects.ts`. Test: typecheck; Vitest.
- [x] **P1-16** Client `ProjectsListPage.tsx` (`/proyectos`; PUBLISHED only; tag chips from `/api/projects/tags`; `?tag=` refetch; i18n neutral Spanish). REQ: projects:ClientPages. Files: `client-site/src/pages/Projects/ProjectsListPage.tsx`, `client-site/src/i18n/translations.ts`. Test: typecheck.
- [x] **P1-17** Client `ProjectDetailPage.tsx` (`/proyectos/:slug`; `sanitizeHtml(body,{allowMedia:true})`, images, repositoryUrl, tags; 404 state) + routes `App.tsx` + ErrorBoundary. REQ: projects:ClientPages + sanitize. Files: `client-site/src/pages/Projects/ProjectDetailPage.tsx`, `client-site/src/App.tsx`. Test: typecheck; sanitize assertion.
- [x] **P1-18** Recruiter `useProjects.ts` path migration `/projects`→`/api/portfolio/projects*`; types `project|laboratorio`; `detailEndpointMap` + `project:'/projects'`; `ProjectSummary.tags?`. REQ: recruiter:Listing/DetailModal. Files: `recruiter-site/src/hooks/useProjects.ts`, `recruiter-site/src/types/index.ts`. Test: typecheck.
- [x] **P1-19** Recruiter `ProjectList.tsx` (classification filter reads project tags + legacy; lab cards → `/blog/:slug`) + `ProjectDetailModal.tsx` real-Project branch (GET `/api/projects/:slug`, sanitized body/images/tags/repositoryUrl). REQ: recruiter:DetailModal. Files: `recruiter-site/src/components/projects/ProjectList.tsx`, `ProjectDetailModal.tsx`. Test: typecheck; Vitest.
- [x] **P1-20** Phase gate: `pnpm -r run typecheck` 0 errors; api coverage ≥70%. Test: full suite.

## Phase 2: Blog tags + admin gaps + uploads (WEIGHT: MEDIUM → optional split: blog-tags chain vs uploads+forms)

- [ ] **P2-01** Prisma `BlogPost.tags String[]` + `migrate dev --name add_blog_tags`. REQ: blog-tags:TagsField. Files: `api/prisma/schema.prisma`. Test: schema inspect.
- [ ] **P2-02** Shared `blogPost.schema.ts`: `tags: tagsSchema.optional()` + filter gains `tag`. REQ: blog-tags:TagsField. Files: `packages/shared/src/schemas/blogPost.schema.ts`. Test: schema tests (empty tags valid).
- [ ] **P2-03** Data migration script: existing `category` → first tag; empty category → no tags. REQ: blog-tags:TagMigration. Files: `api/src/scripts/migrate-category-to-tags.ts`. Test: script run + spot-check.
- [ ] **P2-04** RED `blog-post.service.test.ts`: tags persist on create/update, derived category = first tag, tag+category AND filter, `/tags` PUBLISHED-only. REQ: blog-tags:TagsField/TagFilter/TagSuggestions. Files: `api/src/services/blog-post.service.test.ts`. Test: RED.
- [ ] **P2-05** GREEN `blog-post.service.ts` (+getTags; tag filter `tags:{hasSome}` AND category/search) + controller + routes `GET /tags` before `/:slug` + integration (401, route order). REQ: blog-post-api:Routes. Files: `api/src/services/blog-post.service.ts`, `api/src/controllers/blog-post.controller.ts`, `api/src/routes/blog-post.routes.ts`. Test: GREEN + integration.
- [ ] **P2-06** Update `portfolio.service.ts` lab filter category-proxy → `tags hasSome [laboratorio,experimento]`, articulo excluded; extend regression tests. REQ: recruiter:Listing (final). Files: `api/src/services/portfolio.service.ts` + test. Test: suite GREEN.
- [ ] **P2-07** Admin shared `TagInput.tsx` (free entry, suggestions from `/api/{entity}/tags`, chips, max 10) + adopt in BlogPostForm and Project forms. REQ: blog-tags:AdminTagEditor + projects:AdminPages. Files: `admin-panel/src/components/shared/TagInput.tsx`, `admin-panel/src/components/blog-posts/BlogPostForm.tsx`, project pages. Test: typecheck; Vitest.
- [ ] **P2-08** Admin SuccessCase forms + videos/links (create + edit). REQ: admin-sc:Create/Edit. Files: `admin-panel/src/pages/success-cases/*`, `admin-panel/src/components/success-cases/SuccessCaseForm.tsx`. Test: typecheck.
- [ ] **P2-09** Admin Service forms + includedItems/technicalExplanation (create + edit). REQ: admin-sv:Create/Edit. Files: `admin-panel/src/pages/services/*`, `admin-panel/src/components/services/ServiceForm.tsx`. Test: typecheck.
- [ ] **P2-10** RED→GREEN upload bucket allowlist (`api/src/config/upload.config.ts` — env `UPLOAD_BUCKET_ALLOWLIST` or config constant; decide at apply): `upload.service/controller` honor `bucket` (allowed / unknown 400 / missing default), SVG 400 XSS-safety, correct content-type. REQ: upload:ServerBucket/ContentType. Files: `api/src/config/upload.config.ts`, `api/src/services/upload.service.ts`, `api/src/controllers/upload.controller.ts` + tests. Test: RED→GREEN.
- [ ] **P2-11** Admin `ImageUploader.tsx` DEFAULT_ACCEPT → `image/jpeg,image/png,image/gif,image/webp` (no SVG). REQ: upload:ClientAccept. Files: `admin-panel/src/components/uploads/ImageUploader.tsx`. Test: Vitest accept-attr assertion.
- [ ] **P2-12** Client BlogPage: tag filter auto-populated from `/api/blog-posts/tags`, AND with category/search, URL-param persistence, 300ms debounce. REQ: blog-filters:ClientBlog. Files: `client-site/src/pages/Blog/*`. Test: typecheck; Vitest debounce/URL.
- [ ] **P2-13** Recruiter BlogPage: same tag filter + URL params (requirement names MUST match main spec for archive merge). REQ: blog-filters:RecruiterGrid. Files: `recruiter-site/src/pages/BlogPage.tsx`. Test: typecheck.
- [ ] **P2-14** Phase gate: typecheck 0 errors; api coverage ≥70%.

## Phase 3: Shared editor + sanitization adoption (WEIGHT: MEDIUM)

- [ ] **P3-01** `packages/shared/package.json`: add @tiptap/* + react deps. REQ: rich-text:SharedEditor. Test: typecheck.
- [ ] **P3-02** Shared `RichTextEditor.tsx` (`{value,onChange,minHeight}`; StarterKit h1–4, Underline, Link, TextAlign, Highlight; toolbar extracted from admin TipTapEditor). REQ: rich-text:SharedEditor. Files: `packages/shared/src/components/RichTextEditor/RichTextEditor.tsx`. Test: typecheck; manual init-from-HTML + emits HTML.
- [ ] **P3-03** `InlineImage` node — insert between paragraphs, serializes `<figure><img>`. REQ: rich-text:InlineImage. Files: `packages/shared/src/components/RichTextEditor/extensions/InlineImage.tsx`. Test: typecheck; manual.
- [ ] **P3-04** `InlineVideo` node — local/URL, serializes `<video src>`. REQ: rich-text:InlineVideo. Files: `packages/shared/src/components/RichTextEditor/extensions/InlineVideo.tsx`. Test: typecheck; manual.
- [ ] **P3-05** `SimulatorPlaceholder` node — "Insertar simulador", serializes `<div data-simulator-id>`. REQ: rich-text:SimulatorPlaceholder. Files: `packages/shared/src/components/RichTextEditor/extensions/SimulatorPlaceholder.tsx`. Test: typecheck; manual.
- [ ] **P3-06** Delete `admin-panel/src/components/blog-posts/TipTapEditor.tsx`; adopt shared `RichTextEditor` in Project/Product/Tool/Blog forms (output stays DOMPurify-compatible). REQ: rich-text:Adoption/StorageCompat. Files: admin Project/Product/Tool/Blog forms. Test: typecheck; manual sanitizable output.
- [ ] **P3-07** Sanitization adoption: client + recruiter renderers use `sanitizeHtml(…,{allowMedia:true})` (recruiter: BlogPostContent body/lessons, ProjectDetailModal explanation; client: ServiceDetail + remaining); assert admin-panel has zero `dangerouslySetInnerHTML`. REQ: sanitize:AllRenderers/AdminNoHTML. Files: client-site + recruiter-site renderers. Test: strip/preserve assertions.
- [ ] **P3-08** Phase gate: `pnpm -r run typecheck` 0 errors (5 packages); Vitest where sensible.

## Phase 4: Simulators (WEIGHT: MEDIUM)

- [ ] **P4-01** Prisma `Simulator` model (id, title, slug unique, fileName, size, mimeType, width?, height?, uploadedAt, timestamps, deletedAt) + `migrate dev --name add_simulator_model`. REQ: simulators:Upload. Files: `api/prisma/schema.prisma`. Test: schema inspect.
- [ ] **P4-02** Supabase: create private `simulators` bucket (infra). REQ: simulators:Upload. Test: manual bucket check.
- [ ] **P4-03** RED `simulator.service.test.ts`: upload validation (≤1MB, `.html`/text/html only, auth), record creation, download, size guard, 404. REQ: simulators:Upload/ServingEndpoint/SizeLimits. Files: `api/src/services/simulator.service.test.ts`. Test: RED.
- [ ] **P4-04** GREEN `simulator.service.ts` (upload/list/getMetadata/download with 1MB guard). REQ: simulators:Upload. Files: `api/src/services/simulator.service.ts`. Test: GREEN.
- [ ] **P4-05** `storage.service.ts` + `downloadFile(bucket, fileName)` server-side stream. REQ: simulators:ServingEndpoint. Files: `api/src/services/storage.service.ts`. Test: unit.
- [ ] **P4-06** `simulator.controller.ts` + `simulator.routes.ts` (`/upload` JWT+multer, `GET /`, `GET /:id`, `GET /:id/content` public) + mount `app.ts`; `/content` sets text/html + CSP `sandbox allow-scripts; frame-ancestors <CORS_ORIGIN>; default-src 'none'; base-uri 'none'; form-action 'none'` + nosniff + no-store, removes helmet X-Frame-Options. REQ: simulators:ServingEndpoint. Files: `api/src/{controllers,routes}/simulator.*`, `api/src/app.ts`. Test: integration header/404/401 assertions.
- [ ] **P4-07** Admin simulators page (list + upload form ≤1MB) + `api/simulators.api.ts`; wire into editor picker. REQ: simulators:Upload. Files: `admin-panel/src/pages/simulators/*`, `admin-panel/src/api/simulators.api.ts`. Test: typecheck; manual upload.
- [ ] **P4-08** Shared `SimulatorNode.tsx`: bind placeholder → `<iframe src="/api/simulators/:id/content" sandbox="allow-scripts">` (NO allow-same-origin). REQ: simulators:SandboxIframe. Files: `packages/shared/src/components/RichTextEditor/SimulatorNode.tsx`. Test: DOM sandbox-attr assertion (verify phase).
- [ ] **P4-09** Client/recruiter renderers: simulator node → sandboxed iframe inline; standalone section component with width/height limits. REQ: simulators:Placement/SizeLimits. Files: client-site + recruiter-site components. Test: typecheck; sandbox attr assertions.
- [ ] **P4-10** Phase gate: typecheck 0 errors; api coverage ≥70%; run verify scenarios (sanitize allowlist, sandbox attrs, CSP headers).

## Notes / flagged decisions
1. Phase 1 Project form reuses existing admin `TipTapEditor`; P3-06 swaps it for the shared editor.
2. Phase 1 aggregation filters lab posts by existing `category` (BlogPost.tags does not exist yet); P2-06 switches to `tags hasSome` after the migration — keeps Phase 1 shippable.
3. `sanitizeHtml` util lands in P1-03 (client detail needs it in Phase 1); broader adoption + assertions in P3-07.
4. Design open questions resolved at apply: bucket-allowlist source (env vs config), Simulator width/height defaults (~800×600), whether to seed demo Projects (P1-10).
5. Route-order guard (`/tags`, `/by-id/:id` before `/:slug`) enforced by integration tests in P1-06 / P2-05 / P4-06.
6. P1a included only the recruiter hooks path migration (paths → `/api/portfolio/projects*`); lab-post inclusion in the portfolio aggregation (category proxy laboratorio/experimento, articulo excluded, type `laboratorio`) landed in P1b together with lab card handling in ProjectList/ProjectDetailModal (P1-19). P2-06 will switch the proxy to `tags hasSome`.